#The Code to calculate age of the person based on user input and specific data type as input. 

birth_year = int (input('Birth Year: '))
current = int (input("What's the current year? "))
age = current - birth_year
print (age)
